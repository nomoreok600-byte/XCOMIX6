# Base URL of the headless backend engine. Used at BUILD time (to enumerate
# static manga/chapter routes) and at RUNTIME in the browser (to fetch data and
# proxy images). For production this is the API domain.
NEXT_PUBLIC_API_BASE=https://www.a3555bet.com
