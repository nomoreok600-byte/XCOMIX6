export const metadata = {
  title: "Browse Manga, Manhwa & Manhua",
  description: "Filter by genre, status and type to discover your next favorite series on XCOMIX.",
  alternates: { canonical: "/browse" },
};

export default function BrowseLayout({ children }) {
  return children;
}
