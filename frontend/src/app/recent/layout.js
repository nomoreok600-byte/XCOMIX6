export const metadata = {
  title: "Recently Updated Manga",
  description: "The newest manga, manhwa and manhua chapter updates, refreshed continuously on XCOMIX.",
  alternates: { canonical: "/recent" },
};

export default function RecentLayout({ children }) {
  return children;
}
