export const metadata = {
  title: "Home — Latest Manga Updates",
  description: "Browse the latest updated and most popular manga, manhwa and manhua on XCOMIX.",
  alternates: { canonical: "/home" },
};

export default function HomeLayout({ children }) {
  return children;
}
